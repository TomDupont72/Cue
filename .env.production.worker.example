# Install as /etc/cue-worker.env after replacing every placeholder.
# API_URL and Dagster paths are enforced by compose.prod.yaml.
DATABASE_URL=postgresql://cue:replace_with_database_password@postgres.internal:5432/cue
WORKER_TOKEN=replace_with_the_same_worker_token_as_the_api
